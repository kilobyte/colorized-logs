; KBtin's list of commands.  All entries depend on functions declared as:
;   void XXX_command(char *arg, struct session *ses)
;   struct session *XXX_command(char *arg, struct session *ses)
; depending on whether the function name is prefixed with an * or not.
; The order _does_ matter.  If a partial command is typed which matches
; multiple commands, the first one is used.

*all
*session
*read
*run
*zap
;end    -- parsed in a specific way
;unlink -- parsed in a specific way
verbatim
action
alias
antisubstitute
bell

#ifdef UI_FULLSCREEN
bind
#endif

blank
char

#ifdef UI_FULLSCREEN
condump
#endif

ctime
cr
delay
event
deleteitems
deathlog
echo
explode
foreach
firstupper
help
highlight
history
if
else
elif
finditem
getitem
goto
ignore
implode
info
isatom

#ifdef UI_FULLSCREEN
keypad
#endif

killall
listlength
log
loop
nope
map

#ifdef UI_FULLSCREEN
margins
#endif

math
match
mark
messages
mudcolors
news
path
pathdir
presub
prepad
promptaction
postpad
random
;retab
remark
removeevent
unevent
return
reverse
route
savepath
showme
snoop
speedwalk
splitlist
suspend
shell

#ifdef UI_FULLSCREEN
status
#endif

textin
time
sortlist
substitute
gag
substring
strlen
system
;tablist
;tabadd
;tabdelete
tick
tickoff
tickon
ticksize
tolower
togglesubs
toupper
unaction
unalias
unantisubstitute

#ifdef UI_FULLSCREEN
unbind
#endif

unhighlight
unsubstitute
ungag
unpath
unpromptaction
unmap
unroute
variable
version
unvariable
write
writesession
dosubstitutes
dohighlights
decolorize
atoi
strcmp
verbose
dogoto
changeto
send
timecommands
debuglog

#ifdef HAVE_REGCOMP
grep
#endif

#ifdef UI_FULLSCREEN
retain
#endif
